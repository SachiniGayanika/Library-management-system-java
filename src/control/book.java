package control;


public class book {
	
	public void addBook(String isbn, String name, String author,String cat,String pub,String qty ,String des) {
		model.book b = new model.book();
		b.addBook(isbn, name, author, cat, pub, qty, des);
	}
	
	//public void issueBook(String isbn, String name, String id,String issudate,String duedate) {
		//model.book b = new model.book();
		//b.issuebook(isbn, name, id,issudate,duedate);
	//}
	
	//public void returnBook(String isbn) {
		//model.book b = new model.book();
		//b.returnBook(isbn);*/
	}

